2d engine

To build:
Open ZuesEngine/Engine as a project and build using cmake
Open ZuesEngine/Editor and build using cmake
Run Engine

Engine is a static library used by Editor, and after building the GameApp itself.
